pipeline {
  agent {
    node {
      label 'master'
    }
  }
  stages {
    stage('asdddsad') {
      steps {
        sh 'echo hallo'
        sh 'dotnet --version'
        sh 'ende'
      }
    }
  }
  environment {
    asadde = 'adsada'
  }
}
